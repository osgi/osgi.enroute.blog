#
# OSGi enRoute Blog Manager API Test
#

Bundle-Version: 	1.0.0.${tstamp}
Bundle-Description: \
 This bundle is an OSGi test bundle that tests the enRoute Blog Manager API. Projects\
 that provide this API should configure their bnd.bnd file to run this test so that\
 the providers can test their conformance. This bundle requires DSTest to run so this\
 bundle should be included.

Private-Package: \
	osgi.enroute.blog.test


#
# Defines which classes in this bundle implement the TestCase class
# so they should be run in JUnit. (see ../cnf/build.bnd)
#

Test-Cases: 		${test-cases}

Service-Component: 	*

	
####

-buildpath: \
	osgi.core;version='[4.3.1,6)',\
	org.apache.servicemix.bundles.junit;version='[4.11.0,5.0.0)', \
	aQute.dstest;version=1.1.0, \
	osgi.enroute.blog.api;version=latest

####

#
# We base running this project on using the memory provider
# as reference
#

-runbundles: \
	cnf.run.base, \
	aQute.dstest;version=1.1.0, \
	osgi.enroute.blog.memory.provider;version=latest

#
# The test requires the Transaction Manager and thus
# we must set the Transaction API
#

-runpath: \
	org.apache.servicemix.bundles.junit;version=4.11

